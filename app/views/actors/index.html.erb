<%= render 'nav_show' %>

<%= render 'search' %>

<%= render 'actor' %>

<div class="pagination"> 
  <%= will_paginate @actors_pagination %>
</div>

<script type="text/javascript">
  $('img').insetBorder({
    inset : 10,
    borderColor : '#ffffff',
    speed : 250,
    borderType : 'solid'
  });
</script>